<% content_for :title, "Todas las noticias" %>

<h1>Todas las noticias</h1>

<% @articles.each do |article| %>
<div class="row">
	<div class="large-4 columns">
		<%= image_tag article.header.url(:index), class: "th" %>
	</div>
	<div class="large-8 columns">
		<h4><%= link_to article.title, article_slug_path(id:article.id, slug:article.slug) %><br><small class="text-right"><em><%= l article.created_at, format: :long %></em></small></h4>
		<p><%= article.description %> <%= link_to "Ver más >", article_slug_path(id:article.id, slug:article.slug) %></p>
	</div>
	<hr>
</div>
<% end %>