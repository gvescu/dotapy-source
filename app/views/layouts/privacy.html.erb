<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? "DotaPy – Información de privacidad" : "DotaPy" %></title>
    <meta name="description" content="Información de privacidad en DotaPy">

    <meta property="og:title" content="Información de privacidad" />
    <meta property="og:image" content="/assets/article/fb_large_thumb.jpg" />
    <meta property="og:site_name" content="DotaPy" />

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>

    <%= render partial: 'layouts/social' %>
    <%= render partial: 'layouts/analytics' %>
  </head>

  <body>

    <!-- HOME HEADER -->
    <%= render partial: "layouts/header" %>

    <div class="row">
      <div class="large-12 columns">
        <hr>
      </div>
    </div>

    <div class="row" style="margin-top:30px">    
      <div class="large-12 columns" id="article-body">
        <%= yield %>
      </div>
    </div>

    <!-- FOOTER -->
    <%= render partial: "layouts/footer" %>

  </body>
</html>
